  function concatenate(first, last) {
       var full;
       full = first + last;
       return  full;
  }
        console.log(concatenate("shoaib"," khan"));
         //document.write(concatenate("shoaib"," khan"));

        function secondFunction(){
             var result;
             result = concatenate('shoaib', ' khan');
             document.write (result );
        }
